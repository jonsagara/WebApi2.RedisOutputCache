using System;

namespace WebApi2.RedisOutputCache
{
    public static class RedisChannel
    {
        //private static readonly string _pubSubChannelPrefix
        public static string InvalidateVersionedNumber(string controller, string action)
        {
            // vitalsservice.web.api.controllers-channel-invalidateVersionNumber
            //return $"";
            throw new NotImplementedException();
        }
    }
}
